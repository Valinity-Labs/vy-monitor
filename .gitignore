# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Resumable scan checkpoint for scripts/build-mfc-history.mjs (derived data).
.cache

# TradingView Advanced Charts is licensed and must NEVER be committed to this PUBLIC repo.
# The build downloads it from app.valinity.io and verifies every file
# (node scripts/fetch-charting-library.mjs) — run that for local dev too.
public/charting_library/

# Build output. GitHub Actions builds and publishes the site; nothing is served from a
# committed folder any more.
docs/
